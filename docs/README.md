![Go Version](https://img.shields.io) ![License](https://img.shields.io)
# Architectural Design Records (ADR)

В этом документе описаны инженерные решения, принятые при разработке DevAI.

## 1. Слой управления (Control Plane & State Synchronization)
**(Kill-switch/Quarantine/Sandbox)**  
Этот слой отвечает за управление жизненным циклом агентов и мгновенную синхронизацию политик безопасности между всеми инстансами шлюзов.

### Архитектура данных (Hybrid Storage Model)
Для обеспечения отказоустойчивости и минимальных задержек мы используем **Redis** в гибридном режиме:

*   **Persistent State (Snapshots):**
    Использование распределенных множеств (**Redis Sets**: `blocked_agents`, `sandbox_agents`, `quarantine_agents`).
    *   *Зачем:* При старте (Cold Start) каждый инстанс UAG выполняет `Init()` — загружает актуальные списки в локальную память (RAM). Это гарантирует **Fail-Safe** работу: если Redis упадет после старта, шлюз продолжит защищать систему на основе последнего известного состояния.
*   **Real-time Events (Delta Updates):**
    Использование **Redis Pub/Sub** для трансляции изменений.
    *   *Зачем:* Любое действие в Console API (например, нажатие кнопки "Block") отправляет сигнал в канал. Шлюзы, подписанные на него, обновляют свои локальные мапы за миллисекунды. Это обеспечивает **Zero-latency** реакцию на инциденты без опроса (polling) основной базы данных.

### Режимы контроля (Security Postures)
Control Plane переводит пайплайн обработки запроса в одно из четырех состояний:

1.  **LIVE**: Штатный режим. Запросы проходят через PDP (Policy Decision Point) и исполняются в целевых системах.
2.  **SANDBOX (Dry Run)**: Режим "безопасного обучения". Шлюз имитирует выполнение запроса, возвращая агенту успешный ответ, но фактически перехватывает действие. Данные сохраняются в аудит для последующего анализа (Teacher Mode).
3.  **QUARANTINE (HITL)**: Режим повышенного риска. Любое действие агента автоматически требует **Human-in-the-loop** подтверждения через систему Approvals.
4.  **BLOCKED (Kill-switch)**: Режим мгновенной терминации. Трафик агента отсекается на уровне Middleware с минимальным потреблением ресурсов CPU.

---
# 2. Конвейер выполнения (Execution Pipeline) или жизненный цикл запроса (Request Lifecycle)

Обработка каждого запроса в шлюзе UAG построена по принципу **Sequential Filtering & Enforcement**. Запрос проходит через серию слоев, где на каждом этапе он может быть терминирован или модифицирован.

### Этапы прохождения запроса (The Hot Path)

1.  **Observability Ingress (Tracing)**:
    Каждому входящему запросу присваивается уникальный `Trace-ID`. Если ID передан в заголовках, он сохраняется для корреляции (Correlation ID). Это фундамент для дальнейшей отладки и аудита.

2.  **Security Handshake (Auth & Scopes)**:
    Шлюз проверяет наличие и валидность токена. Ключевая особенность — **Capability Scoping**. Мы не просто проверяем "кто это", мы проверяем, дает ли токен право именно на этот `Capability_ID` (например, `jira.delete`).

3.  **Governance Check (Kill-Switch & Quarantine)**:
    Проверка локального кэша состояний (RAM).
    - Если агент в **Blocked**, запрос обрывается немедленно (403).
    - Если агент в **Quarantine**, управление передается в подсистему **HITL (Human-in-the-loop)** для создания заявки на подтверждение.
    - **Dynamic State Recovery**: Поддержка мгновенной разблокировки агентов (Unblock) через сигнальную шину Redis без инвалидации всего кэша.
 
4. **Policy Decision Point (PDP)**:
    Обращение к `MemoEnforcer`. Система ищет политику (специфичную или глобальную) и определяет итоговый эффект (`Allow`, `Deny`, `Sandbox`). Здесь же проверяются `Constraints` (например, лимиты на сумму транзакции).

5.  **Execution & Reliability (The PEP Layer)**:
    Фактическое исполнение запроса. Если выбран режим **Sandbox**, коннектор вызывается в режиме имитации. Если **Live** — запрос уходит в реальную систему через `ReliabilityWrapper`.
    - *Механизмы:* Circuit Breaker защищает шлюз от "лежачих" систем, а Exponential Backoff решает проблему временных сетевых лагов.

6.  **Async Audit Emission (AgentFS)**:
    Финальный результат (успех или ошибка, задержка в мс, статус выполнения) сбрасывается в неблокирующий канал. Это гарантирует, что медленная запись в БД не увеличит `Duration` ответа для клиента.

---
## 3. Стратегия аудита (AgentFS)

Система **AgentFS** спроектирована как высокопроизводительное хранилище «цифровых следов» (Audit Trail) действий агентов. Главная инженерная задача здесь — обеспечить 100% достоверность данных без ущерба для скорости ответа шлюза.

### Пакетная запись и асинхронность (Batching)
Вместо синхронной записи каждого лога в базу данных (что создало бы «бутылочное горлышко» на дисковых операциях), мы используем **модель асинхронного накопления**:

1.  **Буферизация (Memory Buffer)**: Событие из пайплайна сбрасывается в неблокирующий канал Go (`chan`). Это занимает наносекунды и позволяет шлюзу мгновенно вернуть ответ агенту.
2.  **Группировка (Batching)**: Фоновый процесс (Worker) накапливает события и выполняет запись в PostgreSQL только при выполнении одного из условий:
    *   Накоплено **100 событий**.
    *   Прошло **500 миллисекунд** с момента последней записи.
3.  **Эффективный SQL**: Мы используем множественную вставку (`Bulk Insert`), что в десятки раз эффективнее для IOPS базы данных, чем одиночные запросы.

### Гибкая аналитика (JSONB Storage)
Для хранения полезной нагрузки (`payload`) и ответов от систем мы выбрали тип **JSONB**:
*   **Схема-на-лету**: Мы можем менять состав данных в разных коннекторах (Jira, Slack, CRM) без изменения структуры таблиц БД.
*   **Индексация**: PostgreSQL позволяет строить индексы внутри JSON-полей, что дает возможность ИБ-специалистам мгновенно искать запросы по специфическим параметрам (например, «найти все удаления тикетов, где в теле запроса был проект X»).

### Доказательная база (Evidence Integrity)
Каждая запись в аудите неразрывно связана с:
- **Trace-ID**: Сквозной идентификатор для расследования инцидентов.
- **Duration**: Точное время исполнения, что позволяет использовать аудит как источник данных для **SLO/SLI** (мониторинга качества сервиса).
- **Mode**: Флаг (Live/Sandbox/Quarantine), который четко разграничивает реальные действия и тестовые запуски.

---
## 4. Схема данных и масштабирование (Storage & Scaling)

Архитектура данных DevAI разделена на **Транзакционный слой** (состояние системы) и **Аналитический слой** (логи действий). Это позволяет независимо масштабировать чтение настроек и тяжелую запись логов.

### Схема базы данных (PostgreSQL)
Мы используем нормализованную схему с поддержкой специфичных для Postgres типов данных:

*   **Реестр агентов (`agents`)**: Хранит текущие статусы (Active, Blocked, Quarantine) и метаданные. Индексация по `agent_id` обеспечивает мгновенный доступ при авторизации.
*   **Журнал аудита (`audit_logs`)**: Оптимизированная таблица для высокой интенсивности записи.
    *   **Индексы**: Используются составные индексы `(agent_id, timestamp)` для быстрой отрисовки графиков в консоли.
    *   **JSONB**: Поля `payload` и `response` хранятся в бинарном JSON, что позволяет выполнять аналитические запросы без деградации производительности.
*   **Управление доступом (`policies`)**: Таблица правил безопасности. Поддерживает "Wildcard" записи (`*`), что позволяет настраивать глобальные правила для всей группы агентов.

### Стратегия масштабирования (Scale Strategy)

Система готова к горизонтальному росту (Horizontal Scaling) по следующим направлениям:

1.  **Stateless Gateway**: Инстансы шлюза UAG не хранят уникального состояния. Вы можете запустить 10 или 100 копий шлюза за Load Balancer (Nginx/Envoy). Синхронизация между ними происходит через Redis за миллисекунды.
2.  **Database Partitioning**: Таблица `audit_logs` спроектирована с учетом будущего **партиционирования по времени** (Time-based Partitioning). Это позволит хранить данные за годы, не замедляя работу со свежими логами.
3.  **Read Replicas**: Для консоли управления (Console API) можно подключить Read-реплики PostgreSQL. Это разделит нагрузку: шлюз пишет аудит в Master, а админы смотрят отчеты на Replicas.
4.  **Redis Sentinel/Cluster**: Для исключения единой точки отказа в слое синхронизации, система поддерживает работу с отказоустойчивыми кластерами Redis.

---
## 5. Обработка ошибок (Error Taxonomy)
Реализована таксономия ошибок, позволяющая шлюзу отличать временные сетевые сбои (которые нужно ретраить) от логических отказов безопасности (которые ретраить нельзя).

---
## 🔌 Connectors SDK & Integrations

Платформа использует концепцию **Capability-based Integrations**. Вместо предоставления полного доступа к API (токену), UAG предоставляет агентам доступ к конкретным атомарным действиям (capabilities).
*   **Multi-protocol Support:** Шлюз поддерживает входящий трафик по REST/JSON (порт 8080) и подготовлен к нативной поддержке gRPC-клиентов, обеспечивая унифицированную обработку политик вне зависимости от протокола.

### Архитектура коннекторов
Интеграции реализованы через изолированный gRPC-слой, что позволяет:
- **Полиглотность:** Писать коннекторы на любом языке (Go, Python, Java).
- **Изоляция:** Сбой в коннекторе не влияет на стабильность ядра шлюза.
- **Стандартизация:** Единый контракт для всех типов систем.

### Доступные возможности (Mock Library)
В прототипе реализован демонстрационный набор коннекторов для различных типов корпоративных систем:

| Категория | Система | Capability ID | Описание |
| :--- | :--- | :--- | :--- |
| **ITSM** | Jira | `jira.ticket.delete` | Управление жизненным циклом тикетов |
| **Data** | PostgreSQL | `db.query.execute` | Безопасный доступ к корпоративным данным |
| **Comm** | Slack | `slack.message.send` | Уведомления в рабочие каналы |
| **CRM** | Salesforce | `crm.lead.create` | Операции с клиентской базой |
| **Infra** | Unstable | `unstable.service` | Тестовая каппа для проверки Circuit Breaker |

> **Reliability Note:** Все коннекторы "из коробки" защищены слоем `ReliabilityWrapper`, обеспечивающим **Exponential Backoff Retries**, **Circuit Breaking** и **Rate Limiting** без изменения кода самих интеграций.

### Пример расширения
Чтобы добавить новую систему, достаточно реализовать интерфейс `ExecutionProvider`:
```go
type ExecutionProvider interface {
    Call(ctx context.Context, capID string, payload []byte) ([]byte, error)
}
```

---
## 🛠 Эксплуатация и Reliability (Ops Mindset)

Система спроектирована с учетом требований SRE и Ops для обеспечения стабильности в продакшене.

### 1. Наблюдаемость (Observability)
*   **Сквозная трассировка:** Каждый запрос помечается уникальным `Trace-ID`, который прокидывается через все слои (Middleware -> Engine -> Connector -> Audit). Это позволяет мгновенно локализовать проблему.
*   **Структурированные логи:** Мы используем JSON-логирование через `zap`. Это делает систему совместимой с ELK/Loki без дополнительного парсинга.
*   **Health Checks:** В Docker-compose реализованы нативные проверки (`pg_isready`, `redis-cli ping`), что гарантирует корректный старт и перезапуск сервисов в K8s.

### 2. Устойчивость (Resilience)
*   **Circuit Breaker (Предохранитель):** Если внешняя система (например, Jira) начинает отдавать ошибки, шлюз "размыкает цепь", предотвращая каскадные сбои и экономя ресурсы.
*   **Backpressure:** Асинхронная очередь аудита защищает основной поток обработки. Если база данных аудита замедлится, шлюз продолжит отвечать агентам, накапливая события в буфере.
*   **Graceful Shutdown:** Все сервисы корректно обрабатывают `SIGTERM`, дожидаясь завершения активных транзакций и закрывая соединения с Redis/Postgres.

### 3. Incident Response (Инструментарий отладки)
*   **SQL Toolkit:** В папке `scripts/sql/` лежат готовые запросы для расследования инцидентов и аудита безопасности.
*   **Admin Kill-Switch:** Возможность мгновенно нейтрализовать "взбесившегося" агента без деплоя и перезапуска шлюзов.

## 📊 Метрики и SLO (Service Level Objectives)

Система подготовлена к интеграции с Prometheus/Grafana. Мы выделяем четыре золотых сигнала (Golden Signals) для мониторинга шлюза:

### 1. Latency (Задержка)
*   **uag_proxy_duration_seconds**: Гистограмма времени обработки запроса.
    *   *Зачем:* Отслеживание p95/p99 задержек. Мы должны гарантировать, что наш слой контроля добавляет не более 10-15мс к запросу.
*   **uag_audit_batch_write_duration**: Время записи пачки логов в БД.

### 2. Traffic (Нагрузка)
*   **uag_requests_total**: Счетчик всех запросов с разбивкой по `agent_id` и `capability_id`.
*   **uag_active_goroutines**: Мониторинг использования ресурсов рантайма Go.

### 3. Errors (Ошибки)
*   **uag_errors_total**: Типизация ошибок: `policy_deny`, `kill_switch_block`, `connector_error`, `rate_limit_exceeded`.
*   **uag_circuit_breaker_state**: Метрика состояния «предохранителя» (0 - Closed, 1 - Open). *Критический алерт для Ops.*

### 4. Saturation (Насыщенность)
*   **uag_audit_buffer_utilization**: Степень заполнения внутреннего канала аудита.
    *   *Зачем:* Если буфер заполнен на 80%, значит база данных не справляется с записью логов и нам нужно масштабировать хранилище.

### 🏢 Enterprise Systems Compatibility
Архитектура UAG спроектирована для безопасной интеграции с критически важными системами:
- **IAM/Directory:** Безопасное управление доступом через Capability Scoping.
- **ERP/Financials:** Гарантия целостности данных через Idempotency Keys и Circuit Breakers.
- **ITSM/CRM:** Оптимизация нагрузки на API через распределенный Rate Limiting.
- **Databases:** Безопасный Read-only доступ и Sandbox-валидация деструктивных запросов.

## ⚖️ Архитектурные компромиссы (Trade-offs)

При проектировании платформы были приняты осознанные решения, балансирующие между производительностью, консистентностью и сложностью.

### 1. In-memory Policy Cache vs. Database Polling
*   **Выбор:** Мы выбрали **In-memory кэш** (MemoEnforcer) с "холодной загрузкой" (Refresh) при старте.
*   **Почему:** Прямые запросы к PostgreSQL на каждый вызов агента добавили бы минимум 5-10мс задержки и создали бы риск отказа всей системы при деградации БД.
*   **Trade-off:** Мы пожертвовали *строгой консистентностью* (изменения в админке долетают до шлюза с задержкой в миллисекунды через Redis Pub/Sub) ради *высокой доступности* и минимального Latency (Hot Path теперь занимает < 1мс).

### 2. Асинхронный аудит (AgentFS) vs. Синхронная запись
*   **Выбор:** Асинхронная пакетная запись (Batching).
*   **Почему:** Синхронная запись лога в БД перед ответом агенту сделала бы шлюз зависимым от скорости дисковой подсистемы БД.
*   **Trade-off:** В случае внезапного падения шлюза (panic/OOM), последние 100 логов из буфера могут быть потеряны. Для данной системы это приемлемый риск по сравнению с замедлением критических бизнес-процессов.

### 3. UUID Storage vs. String Representation
*   **Выбор:** UUID на уровне PostgreSQL, string на уровне Go.
*   **Почему:** UUID в БД обеспечивают высокую скорость поиска и компактность индексов (128 бит). Strings в Go упрощают роутинг и JSON-контракты.
*   **Trade-off:** Требуется дополнительная конвертация на уровне Repository, что немного усложняет код, но дает значительный выигрыш в масштабируемости данных.

### 4. Null Object Pattern для Метрик
*   **Выбор:** Гарантированная инициализация объектов метрик.
*   **Почему:** Чтобы избежать сотен проверок `if metrics != nil` в бизнес-логике.
*   **Trade-off:** Небольшое потребление памяти на пустые реестры, если мониторинг отключен, что компенсируется чистотой и безопасностью кода.

### 5. Использование database/sql при драйвере pgx
*   **Выбор:** Использование стандартного интерфейса `database/sql` совместно с драйвером `pgx/v5/stdlib`.
*   **Почему:** Это обеспечивает **инфраструктурную гибкость**. Использование стандартного интерфейса позволяет при необходимости заменить PostgreSQL на другую SQL-совместимую базу (например, CockroachDB) с минимальными изменениями в коде.
*   **Trade-off:** Мы теряем доступ к некоторым специфичным низкоуровневым фичам `pgx` (например, нативным протоколам копирования), но сохраняем чистоту архитектуры и возможность использовать стандартный инструментарий Go для работы с БД.

### 6. Единое хранилище для транзакций и аналитики
*   **Выбор:** Запись аудит-логов в ту же базу PostgreSQL, где хранятся настройки.
*   **Почему:** Для стадии прототипа и MVP это значительно **снижает сложность эксплуатации (TCO)**. Нам не нужно поддерживать отдельный кластер ClickHouse или ElasticSearch, настраивать ETL-процессы и следить за консистентностью между разными БД.
*   **Trade-off:** При экстремальных нагрузках запись логов может начать конкурировать за ресурсы с транзакционной частью. Для решения этой проблемы мы заложили асинхронную пакетную запись (Batching), отдельные интерфейсы и готовность к партиционированию таблиц логов.
